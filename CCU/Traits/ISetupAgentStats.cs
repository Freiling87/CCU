namespace CCU.Traits
{
    public interface ISetupAgentStats
    {
        void SetupAgentStats(Agent agent);
        //void TraitGroupSetup(Agent agent); // Consider adding this, I think there's a good use for it
    }
}